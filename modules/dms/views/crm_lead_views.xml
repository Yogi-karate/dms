<?xml version="1.0"?>
<odoo>
       <record id="crm_lead_lost_view_form_new" model="ir.ui.view">
            <field name="name">crm.lead.lost.form.new</field>
            <field name="model">crm.lead.lost</field>
            <field name="arch" type="xml">
                <form string="Lost Reason">
                    <group class="oe_title">
                        <field name="lost_reason_id"/>
                    </group>
                    <footer>
                        <button name="action_lost_reason_apply_new" string="Submit" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="crm_lead_lost_action_new" model="ir.actions.act_window">
            <field name="name">Lost Reason.</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">crm.lead.lost</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="crm_lead_lost_view_form_new"/>
            <field name="target">new</field>
        </record>

    <record id="crm_case_form_view_leads_new" model="ir.ui.view">
        <field name="name">crm.lead.form.lead.new</field>
        <field name="model">dms.vehicle.lead</field>
        <field name="arch" type="xml">
            <form string="Leads Form">
                <header>
                    <button name="%(action_dms_lead2opportunity_partner)d" string="Book a Service"
                            type="action"
                            help="Book a Service" class="oe_highlight"
                            attrs="{'invisible': [('type', '=', 'opportunity')]}"/>
                    <button name="toggle_active"
                            string="Restore"
                            type="object"
                            attrs="{'invisible': ['|', ('probability', '&gt;', 0), ('active', '=', True)]}"/>
                  <!--  <button name="action_set_lost"
                            string="Mark as Lost"
                            type="object"
                            attrs="{'invisible': ['&amp;', ('probability', '=', 0), ('active', '=', False)]}"/> -->

                    <button name="%(crm_lead_lost_action_new)d" string="Mark as Lost"
                                type="action" class="oe_highlight"
                                context="{'default_lead_id': active_id}"
                                attrs="{'invisible': [('active', '=', False),('probability', '&lt;', 100)]}"/>
                </header>
                <sheet>
                    <field name="active" invisible="1"/>
                    <div class="oe_button_box" name="button_box">
                    </div>
                    <div class="badge-pill badge-danger float-right"
                         attrs="{'invisible': ['|', ('probability', '&gt;', 0), ('active', '=', True)]}">Lost
                    </div>
                    <div class="badge-pill badge-success float-right"
                         attrs="{'invisible': [('probability', '&lt;', 100)]}">Won
                    </div>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only" string="Lead"/>
                        <h1>
                            <field name="name" placeholder="Describe the lead..."/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <!-- Preload all the partner's information -->
                            <field name="partner_id" string="Customer"
                                   widget="res_partner_many2one"
                                   context="{'default_name': contact_name, 'default_street': street, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_function': function, 'default_phone': phone, 'default_mobile': mobile, 'default_email': email_from, 'default_user_id': user_id, 'default_team_id': team_id, 'default_website': website, 'show_vat': True}"
                                   groups="base.group_no_one" invisible="1"/>
                            <field name="partner_name" string="Customer"/>
                            <label for="street" string="Address"/>
                            <div class="o_address_format">
                                <field name="street" placeholder="Street..." class="o_address_street"/>
                                <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                                <field name="city" placeholder="City" class="o_address_city"/>
                                <field name="state_id" class="o_address_state" placeholder="State"
                                       options='{"no_open": True}' domain="[('country_id','=',104)]"/>
                                <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                                <field name="country_id" invisible="1" placeholder="Country" class="o_address_country"
                                       options='{"no_open": True, "no_create": True}'/>
                            </div>
                            <field name="website" widget="url" placeholder="e.g. www.odoo.com" invisible="1"/>
                             <field name="mobile"/>
                            <field name="phone" string="Alternate Number"/>

                        </group>
                        <group>
                           <!-- <field name="service_type" invisible="1"/>
                            <field name="dos"/>
                            <field name="vin_no"/>
                            <field name="registration_no"/>
                            <field name="location_id"/>
                            <field name="due_date"/>
                            <field name="date_follow_up"/> -->
                            <label for="contact_name" invisible="1"/>
                            <div class="o_row" invisible="1">
                                <field name="contact_name"/>
                                <field name="title" placeholder="Title" domain="[]" options='{"no_open": True}'/>
                            </div>
                            <field name="is_blacklisted" invisible="1"/>
                            <label for="email_from" class="oe_inline"/>
                            <div class="o_row o_row_readonly">
                                <i class="fa fa-ban" style="color: red;" role="img"
                                   title="This email is blacklisted for mass mailing"
                                   aria-label="Blacklisted" attrs="{'invisible': [('is_blacklisted', '=', False)]}"
                                   groups="base.group_user"></i>
                                <field name="email_from" widget="email"/>
                            </div>
                            <field name="function" invisible="1"/>
                        </group>
                        <group>
                            <field name="user_id" invisible="1" domain="[('share', '=', False)]"
                                   context="{'default_groups_ref': ['base.group_user', 'base.group_partner_manager', 'sales_team.group_sale_salesman_all_leads'], 'team_id': team_id}"/>
                            <field name="team_id" widget="selection" domain="[('use_leads','=',True)]" invisible="1"/>
                            <field name="type" invisible="1"/>
                            <field name="opportunity_type" invisible="1"/>
                            <!--<field name="remarks"/> -->
                        </group>
                        <group>
                            <field name="priority" widget="priority" invisible="1"/>
                            <field name="tag_ids" invisible="1" widget="many2many_tags"
                                   options="{'color_field': 'color', 'no_create_edit': True}"/>
                         <!--   <field name="pick_up_address" attrs="{'invisible': [('pick_up_address', '=', False)]}"/>
                            <field name="lead_id" attrs="{'invisible': [('lead_id', '=', False)]}"/> -->
                        </group>
                    </group>
                    <notebook>
                        <page name="Vehicle" string="Vehicle">
                            <field name="vehicle_id"/>

                        </page>
                        <page string="Internal Notes">
                            <field name="description"/>
                        </page>
                        <page name="extra" string="Extra Info">
                            <group>
                                <group string="Email" groups="base.group_no_one">
                                    <field name="message_bounce" readonly="1"/>
                                </group>
                                <group string="Tracking" invisible="1"
                                       groups="base.group_multi_company,base.group_no_one" name="categorization">
                                    <field name="company_id"
                                           groups="base.group_multi_company"
                                           options="{'no_create': True}"/>
                                    <field name="campaign_id"/>
                                    <field name="medium_id"/>
                                    <field name="source_id"/>
                                    <field name="referred"/>
                                </group>
                                <group string="Analysis" groups="base.group_no_one">
                                    <field name="date_open"/>
                                    <field name="date_closed"/>
                                    <field name="probability"/>
                                </group>
                            </group>
                        </page>

                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread" options="{'post_refresh': 'recipients'}"/>
                </div>
            </form>
        </field>
    </record>


    <record id="crm_case_tree_view_leads_new" model="ir.ui.view">
        <field name="name">crm.lead.tree.lead.new</field>
        <field name="model">dms.vehicle.lead</field>
        <field name="arch" type="xml">
            <tree string="Leads" decoration-bf="message_needaction==True" decoration-muted="probability == 100">

                <field name="create_date" invisible="1"/>
                <field name="name" string="Lead"/>
               <field name="service_type"/>
                <field name="mobile"/>
                <field name="activity_date_deadline" string="Next Followup Date"/>
                <field name="date_deadline" string="Service Date"/>
                <field name="registration_no"/>
                <field name="activity_summary"/>
                <field name="contact_name" invisible="1"/>
                <field name="city" invisible="1"/>
                <field name="opportunity_type" invisible="1"/>
                <field name="email_from" invisible="1"/>
                <field name="user_id" invisible="1"/>
                <field name="partner_id" invisible="1"/>
                <field name="team_id" invisible="1"/>
                <field name="active" invisible="1"/>
                <field name="probability" invisible="1"/>
                <field name="campaign_id" invisible="1"/>
                <field name="referred" invisible="1"/>
                <field name="medium_id" invisible="1"/>
                <field name="message_needaction" invisible="1"/>
            </tree>
        </field>
    </record>


    <record id="action_new_lead_form" model="ir.actions.act_window">
        <field name="name">Calls</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">dms.vehicle.lead</field>
        <field name="view_ids"
               eval="[(5, 0, 0),
                           (0, 0, {'view_mode': 'tree', 'view_id': ref('crm_case_tree_view_leads_new')}),
                          (0, 0, {'view_mode': 'form', 'view_id': ref('crm_case_form_view_leads_new')})]"/>
        <field name="context"></field>
        <field name="domain">[('type','=','lead'),('activity_ids.user_id','=',uid)]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add a Vehicle
            </p>
            <p>
                create leads
            </p>
        </field>
    </record>

    <menuitem action="action_new_lead_form" id="menu_action_vehicle_lead_form"
              parent="menu_dms_leads_root"
              sequence="1"/>

     <menuitem action="action_service_booking" id="menu_action_vehicle_lead_form_oppor"
              parent="menu_dms_leads_root"
               string="Bookings"
              sequence="2"/>




</odoo>